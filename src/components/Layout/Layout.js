import Form from '../Form/Form';
import Display from '../Display/Display';

const Layout = () => {
  return (
    <div className="mb-4">
      <Form />
      <Display />
    </div>
  );
};

export default Layout;